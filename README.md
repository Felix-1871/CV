# CV templating system

http://jsonresume.org/
## Workflow

- `git submodule update --init` (themes live in `themes/`, EN on `main`, PL on `pl`)
- `npm i -g resumed`
- `npm i` 
- Run bash script or alias it